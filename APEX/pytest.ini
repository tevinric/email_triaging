[pytest]
testpaths = tests
python_files = test_*.py
addopts = --tb=short --cov=. --cov-report=xml:coverage.xml --cov-report=term
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
